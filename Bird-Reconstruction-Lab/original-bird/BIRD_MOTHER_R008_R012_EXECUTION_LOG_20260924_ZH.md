# Bird Mother R0.08—R0.13 连续执行总账

- 记录日期：2026-09-24
- 仓库：`haihao0307/Humanoid-Rig-Lab-Next`
- 来源批次：`seagull (A).zip`
- 来源 GLB SHA-256：`175c61f65e3f2c8b561d422f9d0c61b23bfd85928dd3b66ad8547638d84a90aa`
- 来源许可：`CC-BY-NC-SA-4.0`
- 固定边界：当前所有工作仍是来源学习、运动蒸馏和工程交接，不代表鸟体视觉批准，也没有产生可直接商用的最终鸟资产。

## R0.08｜翼链控制迁移台

- 将来源 50 帧、9 条运动曲线压缩为 8 个来源锚点、72 个锚点参数；
- 450 个逐帧曲线样本缩减为 72 个锚点参数，样本数量减少 84%；
- 建立平滑与线性重放、来源不对称保留、来源翼链与控制器翼链对照和 RMSE 审计；
- 控制器双生体不修改来源鸟体；
- 不继承来源分离羽片，不解锁外翼扇面和未提供飞行姿态；
- 工作流 `35876833016`：部署和桌面/390×844 手机浏览器 QA 全部成功；
- `visualAcceptance=false`，`productionReady=false`。

## R0.09｜通道分离与来源限位

- 将来源运动拆成四组可迁移通道：翼根驱动、翼肘折叠、身体耦合、显式左右不对称；
- 对左右翼根、中翼、外翼、翼肘、平均翼尖高度、翼展、身体高度和镜像 RMS 建立来源包络；
- 左右腕部折角继续保持只读锁定，不把不足约 `0.008°` 的变化冒充有效外翼扇面自由度；
- 建立来源与重放的闭环差、一阶差分、二阶差分、反转点速度跳变和越界计数；
- 工作流 `35890527582`：部署和桌面/390×844 手机浏览器 QA 全部成功；
- 限位只是这一份来源文件的证据包络，不是鸥类关节生物学常数。

## R0.10｜通道执行合同

- 固定显式输入：`phase01`、插值模式、翼根驱动、翼肘/中外翼折叠、身体耦合、来源左右残差、周期缝工程候选；
- 固定输出：11 个活动通道、2 个只读腕部通道和只读诊断；
- 固定输入裁剪、上次有效值回退、默认值回退、锁定通道写入拒绝和来源包络后置检查；
- 非有限值、越界值、错误插值模式、锁定腕部写入和后置越界均有明确处理；
- 后置越界或来源缺失时 fail-closed，不输出伪造运动包；
- 来源三维鸟保持只读，控制器只驱动审计输出；
- 工作流 `35928381635`：部署和桌面/390×844 手机浏览器 QA 全部成功。

## R0.11｜公共相位时钟与长周期稳定性

- 唯一运动定义固定为 `phase01 ∈ [0,1)`；
- 来源周期固定为 `49/24 s`；
- 使用 `360 Hz` 公共主时基，每周期 `17,640` 个整数相位刻度；
- 24、30、60、90、120 Hz 分别以 360、288、144、96、72 个相位刻度采样同一运动；
- `F50_TO_F01` 成为显式边界事件，并保留其余来源事件锚点；
- 建立时钟序列化、恢复、采样率切换和事件序号；
- 建立 1、10、100、1,000、10,000 周期累计漂移审计；
- 关键结论：24 Hz 和 120 Hz 的每周期样本数为整数，30、60、90 Hz 不是；逐周期四舍五入样本数会确定性累积漂移，因此刷新率只能采样统一相位时钟，不能重新定义动作；
- 工作流 `35929474644`：部署和桌面/390×844 手机浏览器 QA 全部成功。

## R0.12｜精确 66 字节运行时交接包

- 纠正早期草案中的两个错误前提：不再使用浮点式 `phaseQ32`，也不保存 `rateMilliHz`；
- 直接保存 R0.11 的精确整数 `phaseTicks` 和受支持的整数 `rateHz`，避免重新近似已经精确的公共时钟；
- 固定 66 字节、小端序运行时包：
  - 8 字节 magic：`BMR01201`；
  - schema 与 flags；
  - `cycleIndex`、`phaseTicks`、`rateHz`、`eventSerial`；
  - 5 个 Q16 控制量；
  - 11 个活动通道；
  - 2 个只读腕部通道；
  - 事件掩码；
  - FNV-1a 32 位校验和；
- 控制量先量化，再由 R0.10 复算通道；解码时每一个 Q16 通道必须与复算结果一致；
- 建立确定性重复编码、解码—重编码、单字节损坏拒绝、重算校验和后的伪造通道拒绝、非法采样率、非法 flags 和 8 个来源事件精确相位检查；
- 最大归一化量化误差限制在半个 Q16 台阶以内；
- 工作流 `35930344100`：部署、桌面端和 390×844 手机端线上浏览器 QA 全部成功；
- 来源鸟体继续保持只读，交接包不会改写来源网格。

## R0.13｜冻结重放一致性语料

- 冻结 40 个 66 字节已知答案包；
- 覆盖 8 个来源事件位置、24/30/60/90/120 Hz 五种受支持采样率，以及 4 组合法控制配置；
- 每个案例固定 packet hex、校验和、精确 `phaseTicks`、周期编号、事件位、控制量和 Q16 通道结果；
- 同时使用 R0.12 解码器与独立 R0.13 参考消费者核对；
- 40/40 案例通过解码、重编码、Q16 通道复算和元数据一致性检查；
- 80/80 单字节损坏路径被两个消费者拒绝；
- 8/8 跨采样率事件组保持相同事件位和精确事件相位；
- 语料 SHA-256：`4a89b13c4b61cad871c7c1511c43b5d6648e30f375083366d3b8ce9b0c200a6a`；
- 工作流 `35951605434`：部署、桌面端和 390×844 手机端线上浏览器 QA 全部成功；
- R0.13 证明的是当前来源合同可被独立消费者逐字节复现，不证明动作在生物学或空气动力学上正确。

## 仍未解决

- 精确物种、真实尺度、年龄和性别；
- 最终身体、喙、腿足与羽毛资产；
- 展向扭转、外翼扇面、初级飞羽展开、次级飞羽重叠和空气载荷响应；
- 滑翔、翱翔、转弯倾斜、起飞、着陆和逃逸；
- 商业运行时资产及视觉批准。

## 当前统一状态

- `visualAcceptance=false`
- `productionReady=false`
- `commercialRuntimeEligible=false`

## 下一道门

R0.14 转回直接服务于后续羽毛替换的形态解剖：对来源 147 个连通组件建立逐组件拓扑账、左右镜像配对、面积与薄片度、主导骨骼、翼根—翼尖展向位置、尾羽位置及采用/仅学习/必须重建/未决四态。只拆解来源，不把分离羽片当作最终羽毛母体，也不补造未提供的飞行姿态。
