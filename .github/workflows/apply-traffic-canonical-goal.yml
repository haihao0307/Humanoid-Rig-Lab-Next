name: Apply Traffic Canonical Goal

on:
  push:
    branches:
      - feature/human-task-workbench-adjustment-v2-fullbase
    paths:
      - .github/workflows/apply-traffic-canonical-goal.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/human-task-workbench-adjustment-v2-fullbase
      - name: Apply bounded source patch
        run: python tools/apply-traffic-canonical-goal.py
      - name: Commit canonical goal fix
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm .github/workflows/apply-traffic-canonical-goal.yml tools/apply-traffic-canonical-goal.py
          git add body/NaturalLocomotion.js
          git commit -m "fix(traffic): preserve canonical unique navigation goals"
          git push origin HEAD:feature/human-task-workbench-adjustment-v2-fullbase
