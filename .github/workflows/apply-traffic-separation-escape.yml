name: Apply Traffic Separation Escape

on:
  push:
    branches:
      - feature/human-task-workbench-adjustment-v2-fullbase
    paths:
      - .github/workflows/apply-traffic-separation-escape.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/human-task-workbench-adjustment-v2-fullbase
      - name: Apply bounded source patch
        run: python tools/apply-traffic-separation-escape.py
      - name: Commit separating-motion fix
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm .github/workflows/apply-traffic-separation-escape.yml tools/apply-traffic-separation-escape.py
          git add body/NaturalLocomotion.js
          git commit -m "fix(traffic): allow safe separating motion from overlap boundary"
          git push origin HEAD:feature/human-task-workbench-adjustment-v2-fullbase
