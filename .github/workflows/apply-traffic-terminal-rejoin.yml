name: Apply Traffic Terminal Rejoin

on:
  push:
    branches:
      - feature/human-task-workbench-adjustment-v2-fullbase
    paths:
      - .github/workflows/apply-traffic-terminal-rejoin.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/human-task-workbench-adjustment-v2-fullbase
      - name: Apply bounded source patch
        run: python tools/apply-traffic-terminal-rejoin.py
      - name: Commit terminal rejoin fix
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm .github/workflows/apply-traffic-terminal-rejoin.yml tools/apply-traffic-terminal-rejoin.py
          git add body/NaturalLocomotion.js
          git commit -m "fix(traffic): preserve canonical goal after terminal detours"
          git push origin HEAD:feature/human-task-workbench-adjustment-v2-fullbase
