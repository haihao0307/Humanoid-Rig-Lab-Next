# Humanoid Rig Lab Next V0.3.0 升级说明

## 解决的两个实机问题

### 黄色程序化人体仍然残留

V0.2.0 隐藏基础拾取壳时仍依赖材质可见性控制。用户实机截图显示，被选中的黄色头部和颈部仍可能被 WebGPU 管线绘制。

V0.3.0 在精细表皮就绪后直接将基础人体 Group 从 Three.js 渲染场景移除。基础 Mesh 继续保存在内存中，并由 Raycaster 直接使用，所以身体触碰与拖动能力保持有效。

### 模块工作台看不到精细表皮

V0.2.0 模块中央使用轻量 Canvas 人偶，完整人体只存在于独立 V8 页面。

V0.3.0 将 V8.2 真实人物视口嵌入全部模块页面。轻量人偶仅作为故障回退。

## 升级步骤

1. 将完整包解压到一个新文件夹。
2. 双击 `start.bat`。
3. 保持命令窗口开启。
4. 浏览器打开后按一次 `Ctrl + F5`。
5. 进入人物蒙皮工作台。
6. 保持“表皮来源”为“自动，精细优先”。
7. 等待状态显示“精细人物表皮已附着”。

## 正常画面

```text
人物蒙皮工作台：精细完整人体
骨骼比例工作台：同一精细完整人体
动作与物理工作台：同一人体，可直接拖动
动画系统工作台：同一精细完整人体
综合预览：同一精细完整人体
```

## 显示模式

```text
表皮：只有一套人体
骨架：只有骨架
同时：一套人体加骨架
```

## 缓存处理

V8.2 使用新的本地状态键：

```text
humanoid-skeleton-editor:v8.2-unified-surface
```

旧状态仍会作为迁移来源读取。页面仍显示旧画面时，先按 `Ctrl + F5`。仍未更新时关闭全部本地项目标签页，重新运行 `start.bat`。
