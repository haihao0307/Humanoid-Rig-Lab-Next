# Humanoid Rig Lab Next V0.4.0 升级说明

## 升级目标

V0.4.0 针对实机截图中出现的三个基础问题进行重构：

1. 点击关节时出现黄色第二层人体。
2. 肩膀、腋下和上臂在姿势变化后产生异常隆起与拉扯。
3. 骨架关节中心与当前人体网格的腕、膝、踝、脚部位置不一致。

## 一套人物表面

V8.3 删除以下运行对象：

```text
程序化人体表皮
隐藏选择人体
基础表皮回退 Mesh
黄色表面选中材质
基础人体拾取代理
```

场景中只创建：

```text
MeshcapadeSampleHumanSurfaceSingleMesh
```

同一 Mesh 负责显示、拾取和姿势变形。选择反馈只会改变骨架关节球、骨杆和三轴手柄。

## 精细网格直接拾取

鼠标命中人体三角形以后，系统读取该三角形顶点对应的 `skinIndices` 和 `skinWeights`。三个顶点的权重被汇总，权重最高的 SMPL 关节成为当前选择。

这套流程不依赖第二个不可见人体，因此 WebGPU 和 WebGL 后端都只处理一套人体网格。

## 绑定姿势保护

A 姿势与当前绑定骨架重合时，表皮直接恢复：

```text
原始 POSITION
原始 NORMAL
```

该模式跳过近似蒙皮变形，可以避免初始姿势出现肩部球状凸起、胸部边缘裂缝和关节接缝。

## 人体区域隔离权重

权重生成器将身体划分为：

```text
躯干
左臂
右臂
左腿
右腿
```

每个区域只允许使用兼容骨链。肩部和髋部保留有限过渡区。左臂顶点不会获得右臂权重，左右腿也保持隔离。手部附近增加下肢候选边界，防止 A 姿势中的手掌顶点混入髋部和腿部权重。

初始权重生成后，系统沿 GLB 的真实三角形邻接图执行 3 轮有限平滑。每轮保留顶点自身权重，并混合相邻顶点的骨骼影响，然后重新筛选最强的四个影响。该步骤用于减小肩部硬接缝、细长三角形尖刺和肢体边界的不连续。

## 双四元数变形

姿势变化使用每顶点最多四个关节的 CPU 双四元数变形。相较线性矩阵混合，它能够更好地保留肩部、髋部和弯曲关节附近的体积。

当前权重由运行时规则计算，肩胛、腋下和极端姿势仍需用户进行实际画面审查。正式生产阶段建议接入专业绑定权重和姿势修正形变。

## 骨架拟合数据

标准骨架更新为 `smpl-male-surface-fit-1796-v2`。主要关节世界位置如下：

```text
骨盆中心    [ 0.000, 0.925,  0.016 ]
上胸        [ 0.000, 1.335,  0.002 ]
颈根        [ 0.000, 1.490, -0.010 ]
头部中心    [ 0.000, 1.630,  0.055 ]

左锁骨      [-0.100, 1.405, -0.005 ]
左肩        [-0.210, 1.340, -0.020 ]
左肘        [-0.350, 1.105,  0.025 ]
左腕        [-0.435, 0.900,  0.120 ]
左手节点    [-0.463, 0.840,  0.145 ]

左髋        [-0.100, 0.925,  0.016 ]
左膝        [-0.110, 0.500,  0.002 ]
左踝        [-0.160, 0.100, -0.002 ]
左前脚掌    [-0.176, 0.035,  0.120 ]
左脚趾末端  [-0.176, 0.020,  0.195 ]
```

右侧使用镜像数据。

## 数据迁移

母平台 `schemaVersion` 升级到 `4`，骨架导出 `schemaVersion` 升级到 `5`。

旧字段迁移规则：

```text
skin.source = auto 或 base        转换为 detail
skin.activeSource                 固定为 detail
skin.fallbackAsset                删除
skin.singleLayer                  固定为 true
skin.pickingSource                detailed-smpl-mesh
skin.deformation                  region-isolated-dqs
skin.bindPoseProtection           true
```

## 升级操作

推荐使用完整包并解压到新目录：

```text
Humanoid-Rig-Lab-Next-v0.4.0-anatomical-surface
```

然后：

```text
1. 关闭旧版浏览器页面和旧命令窗口
2. 双击 start.bat
3. 保持命令窗口开启
4. 浏览器打开后按 Ctrl + F5
5. 进入人物蒙皮工作台
6. 先选择“表皮”模式检查唯一人体
7. 再选择“同时”模式检查骨架位置
8. 切换 A 姿势和 T 姿势检查肩部、腕部、膝部和足部
```

## 验收重点

```text
点击关节时人体表面不出现黄色块
表皮模式只显示一套人体
同时模式只显示一套人体和一套骨架
A 姿势肩部保持原始网格形状
T 姿势腋下无大面积三角形拉扯
腕关节位于手腕内部
膝关节位于膝部中心
踝关节和前脚掌骨链位于足部内部
```
