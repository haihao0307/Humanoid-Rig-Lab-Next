# User delivery policy

The current instruction is to maintain one local project, consolidate the active functionality, remove obsolete local project versions and upload the final source to the existing GitHub character repository. The user will launch and inspect it personally.

The current local follow-up is the R11 character-system review: compare the behavior and character modules with conventional character systems and make targeted improvements. Keep the R9 pinned motion integration and R10 surface continuity work. The R11 review and scope are recorded in `docs/CHARACTER_SYSTEM_REVIEW_R11.md`.

Upload and deletion of other project directories remain paused. The earlier R9 instruction replaced the workbench motion system with the independently tuned `Human-Motion-Lab` R2.2 core, including its initial stance, then reconnects the previous action repertoire while preserving long tasks and different behavior forms. This R9 integration keeps the Visual R2 character and its source rig, pins byte-identical lab modules in the workbench, and leaves the independent lab unchanged. Complete the local integration and file audit; do not resume upload or old-directory cleanup without a subsequent instruction.

Use the single `启动人物项目.cmd` Windows entrypoint and the assembled `index.html`. Do not create additional release directories or ZIP copies unless requested. Keep necessary function parameters, source provenance and current feature documentation in this project.

Verification is file-only: source assembly, syntax parsing, dependency inspection, parameter-container validation and hashes. Do not open a browser, operate the user's mouse, generate screenshots, run character actions or claim runtime/visual acceptance without a later user instruction.

While inspection is pending, finish with the local entrypoint and the actual file-check result. Report a GitHub result only after upload has been authorized to resume and verified. Preserve previous commits through normal Git history; do not force-push or remove unrelated project branches.
