# V4.6 R9.1 执行与 QA 报告

## 目标

用户指出头顶仍不正确。本轮在 R9 冻结基线上，自行截图审查并修正：

- 头顶二次隆起；
- 额线到喙根的坡度断裂；
- 鸡冠由分离尖块主导；
- 冠根与头顶的突兀连接；
- 头顶部位被过宽红色材质掩码覆盖。

## 方法边界

本轮使用的是可见表面候选，不把轮廓直接升级为内部颅骨真值。当前模型的品种、性别、年龄和真实米制尺度仍未知。单冠类别证据支持“连续中线叶片及锯齿”，但不支持把当前五个锯齿、具体峰高或峰距宣称为所有家鸡的固定数据。

## 几何修改

### 头顶包络

在现有主体规则拓扑上，仅对 `X=0.270–0.478` 的背侧头面带做有界重拟合。新的目标曲线由 11 个平滑节点控制，侧面面颊、下喙及颈部下缘不参与该场。

### 鸡冠

鸡冠重建为一个连续中线薄叶片：

- 121 个纵向截面；
- 每截面 28 个角向采样；
- 5 个圆钝锯齿候选；
- 冠根轻微进入头顶；
- 前后端逐渐收束；
- 零退化三角面。

### 材质分区

头顶恢复羽被身份；红色软组织限制在面部候选、鸡冠、肉髯和耳叶区域。材料分类仍使用对象局部坐标和局部法线，不使用世界位置刷固定颜色。

## 浏览器验证

默认形态完整扫描：

```text
HTTP 200
131,948 vertices
184,512 triangles
allFinite=true
allIndices=true
totalDegenerate=0
combPassed=true
console/page/request errors=0
```

头部及鸡冠边界组合：

```text
cranial_all_min                         passed
cranial_all_max                         passed
wide_body_small_head_high_comb          passed
narrow_body_large_head_low_comb         passed
```

每组均检查有限值、索引范围、采样退化三角形、鸡冠完整性、腿足审计和尾部审计。

回退验证：

- `headTopR91=0`；
- 17 个网格；
- Float32 位置位模式哈希一致；
- Uint32 索引哈希一致；
- 与冻结 R9 完全匹配。

移动端：

- 390×844；
- 画布 390×455.77；
- 面板 390×388.25；
- 头部近景、固定视图、缩放、重置和材质按钮均可点击；
- 页面错误、警告与请求失败为 0。

SwiftShader 仅用于浏览器正确性检查，不代表实体手机、Safari 或最终 GPU 性能。

## 自检结论

局部改动有效，可以保留。前后对照显示旧版额部峰已被压平，冠根—额部—喙根更加连续。鸡冠不再是互相独立的尖锥。

但本轮不能被描述为“整只鸡已经完成”。六视图仍显示颈部偏长、头部相对偏小、前胸偏满，翼、尾与腿足的近景质量也没有完成。因此只通过技术门与头顶部位候选门，整体视觉门保持关闭。
