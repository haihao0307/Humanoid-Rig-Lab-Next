# 人物动作收敛 R1 基线（2026-09-19）

## 1. 固定基线

- 仓库：`haihao0307/Humanoid-Rig-Lab-Next`
- 工作分支：`feature/human-motion-convergence-r1`
- 分支起点：`main@3c3e9a4b7b250f4c8db20c15e2e5fab4ca9ce568`
- 动作候选来源检查点：`fix/motion-contact-handoff-20260916@c8d1fd0b4d25e87e784d3491c5a4d4391b8751bb`
- 对应历史草稿：PR #7

本分支从统一 `main` 干净起步，只迁入 PR #7 中经过筛选的动作、接触、力量反馈和动作测试内容。服装、面部、外观和无关人物体型改动不随动作迁入。原 PR #7、`main`、任务 PR #8 和锁定的 Motion-Lab vendor 均保持不动。

## 2. 当前事实边界

已经按文件边界迁入并由干净入口回归：

- Human-Motion-Lab R2.2 固定步长步态；
- 完整骨架姿态提交、固定骨长、脚锚和脚掌方向接管；
- 连续步态相位、走转停候选、自由手放松；
- 地面手掌、骨盆和左脚的坐起支撑候选；
- 动作预检、失败回滚、轻量平衡和力量输入；
- 箱体倾转、换手到底托及相关接触检查；
- 只读 `MotionRuntimeState`，统一报告动作意图、目标姿态元数据、支撑、接触、最终姿态证据和运行遥测。

仍未通过：

- 整体动作视觉验收；
- 坐起时完整脚掌、重心和支撑转换；
- 无把手箱体抬起、搬运、放下和释放闭环；
- 人体完整反作用力、地面反力和全身动力学；
- 密集多人实时容量；
- 公网固定工作台的当前提交验收。

特别保留当前真实阻断：箱体换到底托后进入抬起约 0.125 秒，左腕屈伸约 -72.7°，动作保护触发并回滚。不得通过简单放宽腕部上限掩盖真实物体姿态与预检路径不一致的问题。

## 3. 本轮不变量

1. 动作不得修改绑定父子层级、固定骨长或人物比例。
2. 非根骨骼不得使用缩放轨道解决动作问题。
3. Motion-Lab `motion/vendor/` 八个锁定模块保持字节身份不变。
4. 任务系统只提供动作意图和世界约束，不直接写骨架。
5. 人物表皮、碰撞体和服装只能读取最终提交姿态。
6. 文件检查、CPU 回归、浏览器执行和视觉验收必须分开记录。
7. 数值通过不得升级为自然度、生产就绪或完整动力学通过。
8. 每次修改必须保留确定性回滚和旧基线可复现性。
9. `MotionRuntimeState` 只读现有权威状态，不成为第二套求解器，不向骨架、任务、物理、表皮或服装回写。

## 4. 第一阶段执行顺序

### R1.0 基线、筛选和门禁

- [x] 从 `main@3c3e9a4` 建立独立动作分支；
- [x] 新增 `motion-convergence-gate.yml`；
- [x] 按文件边界迁入 PR #7 的纯动作实现和测试；
- [x] 明确排除 `clothing/`、面部、外观和无关体型改动；
- [x] 构建生成入口并核对 source assembly、文件审计和 SHA256；
- [ ] 真实浏览器视觉 QA 单独执行，不由 CPU 门禁代替。

R1.0 已完成的实际记录：

- 纯动作生产迁移提交：`fc7e383940e9dc1ec02eedc82cc9e7349c1c5bc5`；
- 动作回归与历史证据迁移：`af57d675d2cca6f6bb2588dcc99c809b086fc9d6`；
- 接触支撑查询和干净表面审计修正：`beed72509bcea26cc1fc745b5a2ba960815f8258`、`50ae23342e6e5bfe8b70f030ede664b831af70d7`；
- 精确生成入口提交：`e94ae0dc788d59dc28de7ee97912a0409773a201`；
- 入口 SHA256：`78c5b334531a8bc2fc471c3ccecc9c14b5465ba850fa38d6af567e6cbad3be84`；
- 生成入口大小：`1,753,329` bytes；运行时代码：`3,053,557` bytes；
- GitHub Actions `Motion convergence build` 运行 `35452963956` 通过；
- 文件装配、审计、锁定 Motion-Lab、步态相位、脚锚、回滚、自由手、地面掌部支撑和箱体候选回归均通过；
- 当前结果仍不等于真实浏览器视觉通过、完整搬运通过或生产就绪。

### R1.1 显式动作运行状态

- [x] 新增 `MotionRuntimeState` 只读状态层；
- [x] 统一报告 `MotionIntent`、`DesiredPose` 元数据、`SupportState`、`ContactState`、`FinalPose` 证据和 `MotionTelemetry`；
- [x] 接入 `NaturalLocomotion` 的构造、命令意图、逐帧观察、快照、回滚和报告边界；
- [x] 保留现有 Motion-Lab、IK、接触、平衡、任务和物理所有权，不改变动作求解顺序或外观；
- [x] 独立验证状态读取不修改运动内核，快照/恢复逐项一致；
- [x] 重建单文件工作台并通过原动作、接触和回滚回归。

固定报告顺序为：

`MotionIntent → DesiredPose → Contact/IK → Joint Limits → Balance/Load → FinalPose → Skin/Clothing/Collision`

R1.1 实际记录：

- 状态模块提交：`bd87b9d6ddb31d10ffa76965cceecd165b275869`；
- 状态接线与可重复装配脚本最终提交：`dbea136f9b2e22f21467ab041ba27d309a99d5fe`；
- 独立只读、意图、支撑、接触、最终姿态和回滚测试提交：`4fbf4e3ca8b6cd7186752d0a7d99cb6f6ce22dd3`；
- 精确生成入口提交：`1b47814ac8b99cafd3292c38c1edea96cbd428e8`；
- 入口 SHA256：`b73890a3bfba15a7a311299580a78b8eb067cd0662aeba4f33b0784eba87e979`；
- 生成入口大小：`1,756,173` bytes；运行时代码：`3,061,198` bytes；
- 文件数量：`384`；未解析标识符、模型文件和图片文件均为 `0`；源码装配一致；
- GitHub Actions `Motion convergence build` 运行 `35453398171` 通过；
- 装配、入口重建、文件审计、只读状态测试、Motion-Lab、步态相位、脚锚、失败回滚、自由手、地面掌部支撑和箱体候选回归均通过；
- 本轮没有改变动作曲线、脚步、IK、关节限制、平衡参数或物体操作路径，因此不宣称自然度得到改善；
- 本轮没有执行真实浏览器视觉验收，没有解决箱体抬起阻断，也没有建立完整动力学。

### R1.2 走、转、停收敛

在显式状态接线通过后，依次处理：

1. 起步前脚步释放与骨盆前移顺序；
2. 经过路点与最终停止点区分；
3. 连续转向角速度、角加速度和落脚预看；
4. 减速、最后一步、双支撑和上肢回落；
5. 脚跟、全脚掌和脚尖滚动状态；
6. 支撑脚位置/方向漂移及停步残余动作。

### R1.3 肩肘手与坐起

走转停通过后，再处理锁骨、肩胛、肘平面、腕部和手指；随后重做臀部、手掌、脚掌和骨盆的坐起支撑切换。

### R1.4 物体交互与任务联调

最后修复真实箱体姿态进入抬起可达余量，完整跑通接近、抓边、倾转、换手、抬起、行走、转向、放下、抽手和释放；再与任务 PR #8 的导航、让行、暂停和恢复组合测试。

## 5. 当前验收口径

本分支当前只能称为“动作收敛工作基线”。只有同时满足以下条件才可申请合并：

- 文件和锁定来源检查通过；
- CPU 动作、接触和回滚回归通过；
- 真实浏览器冷启动及固定动作链通过；
- 正面、侧面和斜后方连续视觉复查完成；
- 已知硬阻断被解决或明确拆出；
- 与任务分支的共享接口和组合回归通过；
- `visualAcceptance` 仍由用户真实查看后决定。

## 6. 三维交付检查

- [x] 没有用生成图片代替真实三维实现。
- [x] 已实际修改动作生产源码。
- [x] 当前分支包含由干净源码重新生成的可交互三维工作台入口。
- [x] 人物几何、骨骼和动作来自真实运行时。
- [x] 镜头、选择、动作和参数控制沿用现有工作台。
- [ ] 当前收敛提交的公网固定链接和真实浏览器尚未重新验证。
- [x] 只有截图而没有工作台的交付自动判定失败。
