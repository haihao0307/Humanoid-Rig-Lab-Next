# 统一项目整合记录（R6 修正）

2026-09-11。固定项目目录为 `Human-Workbench`，唯一网页入口为根目录 `index.html`。此文件记录来源和整合范围，不是另一份运行版本。

| 来源 | 核对结果 | 本版采用内容 |
| --- | --- | --- |
| NPC 日常 R4 项目 | `index.html` SHA-256 为 `3c630edbf0fe8e6ef519f738f475855770178ae3b58d412e693a52142fb3acd2` | 最新 NPC 日常、动作 R3.1、训练场、程序人体、毛发、语言及本机服务源码 |
| 紧凑训练场 | `index.html` SHA-256 为 `bc00aa49a9d466c6aafff6cc2d0b178633cc7acbde8e6018112336cf3e382577` | 七个紧凑参数包、曲面求值模块和性能面板；此来源自身不含图二的 R2 毛发 |
| 图二的视觉修正 R2 | 四项毛发输入及哈希固定在 `reconstruction/appearance.json` | 原始 R2 布根和梳理函数、种子与参数、头皮标量半径场、419 个头面曲面域的关系和边界范围；眼部使用 R2 原始中心和法向 |
| GitHub 人物库 main | 整合前为 `7c19287631bf5a0c3809c3dfe34fbc33f835d834` | 保留仓库身份和提交历史，通过正常提交更新 |

两份本地页面内容不同。紧凑训练场具有重建曲面和性能面板；NPC R4 的动作和日常代码更新。整合采用最新可读源码装配，不将两个旧版 HTML 互相嵌套，也不保留第二份工作台入口。

七个曲面参数包保持原字节和 SHA-256，合计 13,027,949 字节。浏览器通过模块 Worker 生成临时曲面缓冲，沿用同一 Agent 的骨架、训练场和动作。数据来源、近似处理和许可保存在 `docs/reconstruction/SOURCE_LOCK.json` 与根目录 `THIRD_PARTY_NOTICES.md`；当前容器清单为 `reconstruction/parameters.json`（相对于项目根目录）。

R5 存在两项合并遗漏：没有接入图二的 R2 毛发，且启用毛发、进入局部检查或切换预设会把显示切回原程序人体。R6 将重建人物设为固定默认入口，毛发随重建身体一起加载。原程序结构只作为手动选择的对照视图，体型编辑仍作用于该程序人体。

R5 的绑定直接把参考曲面映射至原骨架的尺寸，可能改变初始外形。R6 保存同一 Agent 的初始关节帧，动画使用当前帧相对初始帧的变化，加上统一的场景位置偏移；身体和发丝共用头部变换。动作绑定仍为候选，未进行动作或接触验收。

R2 发丝在当前头面曲面上重新生成，每根 16 段，不读取历史发丝缓冲。历史图二为 87,391 根，本入口显示实际生成数量。曲面精度切换保留同一份毛发；旧毛发和重建人物分配不同纹理槽。默认流畅渲染，可在面板开启阴影。

Windows 统一使用 `启动人物项目.cmd`，保留同一 Python 本机服务和可选语音功能。移除旧启动器、旧 ZIP 打包工具与另一版本的体积报告；文件审计工具只更新当前目录的检查结果和哈希清单，不生成副本。

验收范围为源码装配、语法、接口引用、七个参数容器和四项 R2 毛发输入的结构与哈希，以及毛发曲面域与紧凑头部参数的对应关系。没有启动网页、操作鼠标、运行人物动作、导航模拟或 GPU；旧截图中的外观、耗时、体积和帧率不构成本版实测证据。

用户已要求先查看本地文件；上传 GitHub 与删除旧目录保持暂停。
