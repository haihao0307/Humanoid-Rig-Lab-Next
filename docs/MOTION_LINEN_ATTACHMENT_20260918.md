# 亚麻裙动作接入与腰部附着

用户已将服装改为亚麻裙，本轮沿用其裙型及材质继续动作衔接。动作分支从 cf2c560 继续，已核对 main 3c3e9a4 是本分支祖先。读取用户已提交的 upload/human-workbench-20260915 / 8b34db7 中的 LinenMaterial.js 和 ProceduralLinenSkirt.js 作为服装组件，适配到 clothing/，没有合并另一板块的分支历史、覆盖其工作树或导入旧动作运行时。

## 保留项和实际修正

八片亚麻裙、12,704 个三角形、内外层、翻边、腰头、约 40.5 cm 原裙长及 3 倍布纹保留；LinenMaterial.js 与来源文件字节一致。默认 CompactSurfaceRenderer 在独立服装系统内创建亚麻裙，旧 skirt 诊断引用兼容，原人物和任务调度不变。

旧腰头只按骨盆关节变换，但实际腰腹皮肤会随腰椎和混合权重移动。新增窄带同源附着场：96 个圆周方向，裙长比例 0、0.07、0.22、0.4 四层；复制邻近且属于躯干/骨盆/大腿的皮肤权重，计算双四元数变换与肌肉形变相对原骨盆变换的位移。腰头完全采用，向下平滑退出。静止时间的腰部骨骼编辑也能使服装刷新，阴影/颜色绘制不重复推进状态。只更新附着场所需的关节变换。

这解决的是腰部跟随权威不一致，不是把服装贴到任意碰撞表面。材质文件没有改动，没有隐藏皮肤、删裙片、改动作或用镜头遮挡来规避穿插。

## 被拒绝的接触方案

真实基线截图在坐姿和起身时仍有腿部与裙面的交叉。最近点大腿投影可能把相邻点推向不同侧；改为裙面外侧投影后出现上提硬片，因此未保留。另一轮 1,008 节点、带牵系约束的布面试验中，初版坐起最大局部边长比例约 18.89 和 22.88，明显不合格；补身体其他代理也未作为默认接受。失败的亚麻动态代码已撤出生产文件，仅保留仓库外试验记录。上一阶段草裙候选求解器未改变。

目前仍继承原大腿几何净空，坐起穿模未解决。下一步必须先对实际变形皮肤拟合分段碰撞体，检查每个固定腰点与身体代理是否相容，再约束拉伸、翻转并补连续碰撞。球/胶囊代理的负间隙不是精确皮肤穿透深度；只通过顶点检查也不保证三角形表面不相交。

## 检查记录

最终入口 SHA256：4835cdaec4ef746cdc22945b95158135bcf46e5bb02b4bcdd0fc0326cc6d1ab6。数值与截图证据目录为仓库外 G:/Three.js/Human/motion-linen-contact-20260918/，其中 baseline 为原亚麻形变，waist-outward、waist-layer、tether-drape、tether-full-contact 为未接受试验，final 和 runtime-final 为最终构建复核。以各报告实际哈希和结果为准，旧截图不代替最终检查。

- 文件构建、装配检查、工具语法和差异检查通过，文件审计交付前刷新。
- 同源附着测试 512 个样本，包含绕腰部旋转、统一世界平移及编码坐标解码；浮点纹理导致的最大目标差约 6.71e-9 m。该夹具旧骨盆独立变换差约 0.17623 m，仅为合成回归，不是人体实测。
- 服装实例、释放、同帧相同姿态不重算、同帧腰部变化触发重算等检查通过。没有用合成无碰撞测试证明真实衣物质量。
- 最终入口的真实浏览器通过 7 类单人动作、6 人同时挥手及 6 人坐下/起身，页面错误 0。多人仍采用 20×16 米空场地、3 米间距，未测密集人群或性能容量。
- 最终服装诊断覆盖坐下、起身、行走、转身共 164 个姿态，采集 68 张实际 WebGL 截图；页面错误 0，动态布料未启用。腰头相对旧骨盆单骨变换的最大修正约 82.074 mm，这是同源绑定差值，不是已消除的穿透深度。
- 亲自查看最终坐姿、起身中段和步行侧面。坐姿裙面与腿部仍交叉，行走时前臂可穿入裙侧；整体服装碰撞未通过视觉验收。补充全身代理的动态试验最大边长比例约 32.24，亦已拒绝。下一步除身体代理外，还需协调自由摆臂与服装外廓间隙，显式手部任务接触应保持优先。

## 共享接口

输入仍是已提交人体骨架、同源蒙皮调色板、身体时间和体型，输出仅服装显示及 attachment 诊断（绑定来源关节、样本数、位移上限）。poseJointIds 是服装可选依赖列表，缺省保留旧行为；需要支持原地改姿态的另一板块应通过正常人体提交/刷新路径更新，不直接移动服装。

任务系统无需改变目标、路线或调度。服装维护方后续同步应以 8b34db7 的两个组件为来源，注意本分支已迁入 clothing/，避免同时装配两份同名类或用旧 skirt.draw 绕过统一生命周期。任何新版碰撞代理应与腰部固定点使用同一实际身体姿态，动态布料不回写动作骨架。

- [x] 没有用生成图片代替真实三维实现。
- [x] 已实际修改生产源码。
- [x] 本机可交互三维工作台可访问。
- [x] 人物、骨骼、服装和动作来自真实运行时。
- [x] 镜头、动作和参数控制可以实际操作。
- [ ] 公网固定链接和真实浏览器已验证（未部署公网工作台）。
- [x] 如果只有截图而没有工作台，本轮判定失败；交付包含源码、生成入口及检查工具。
