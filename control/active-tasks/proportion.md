# 骨骼比例当前任务

1. 已完成当前 `rig@0.4.0` 的 28 节点角色分类。24 个 SMPL 节点标记为变形关节，`root` 标记为全身控制器，`headTop` 与左右脚趾末端标记为测量节点。
2. 已将头顶和脚趾末端测量节点改为主视图默认隐藏。左右锁骨继续保留为变形关节，锁骨骨杆继续显示，锁骨控制球继续隐藏，左右真正肩关节继续可见。
3. 已为全部 28 个节点生成 `humanoid_rig/joint_axes@1.0` 绑定轴契约，包含扭转轴、弯曲轴和侧向轴。当前契约已经完成正交性、完整性和比例重建一致性测试，运行时仍需由动作与动画适配器正式消费。
4. 已在比例工作台增加骨架系统能力审计，可查看变形关节、控制节点、测量节点、默认可见节点、隐藏节点和关节轴完整度，并可导出当前审计与升级清单。
5. 已冻结追加式骨架升级蓝图。身体生产版目标为 32 个变形关节，新增 8 个长骨扭转关节；完整表现版目标为 65 个变形关节，继续加入 30 个手指关节、双眼和下颌。
6. 当前活动拓扑继续保持 24 个 SMPL 稳定 ID 与 28 节点顺序，尚未启用新增扭转骨、手脚 IK、足底接触、手指和面部节点，避免在蒙皮、动作和动画板块完成兼容前破坏现有资产。
7. 下一步需要由总控冻结 `rig@0.5.0` 的追加节点 ID、父子关系、局部轴和导出规则，并组织蒙皮、动作、动画三个板块完成兼容评审。
8. 待共享视口补充按 `role` 与 `visibilityLayer` 切换显示层，待共享导出器完整序列化关节角色字段，待动作运行时保存并应用真实局部四元数与轴向扭转。
