# 人物蒙皮当前任务

1. 用户实机截图已经确认浏览器仍可能进入历史构建。截图中的状态卡文字与 `skin-v001`、`skin-v002` 运行时标记不一致，根目录共享启动器只检查 `4173` 上是否存在同名网站，没有核对工作包路径和构建 ID。
2. `skin-v002` 新增专用启动入口 `src/modules/skin/OPEN_SKIN_V002.bat`。它使用 `4192` 到 `4210` 的独立端口区间，复用端口前必须读取 `skin-build.json` 并确认 `skin-v002-single-surface-guard`。
3. 编辑器加载前会清理场景中已有的历史程序化人体、旧静态人体和拾取壳。运行期间每 160 毫秒继续执行场景级表皮审计，延迟挂载的重复 Mesh 也会被移除。
4. 当前唯一预绑定 GLB 继续使用一个原生 `SkinnedMesh`、`JOINTS_0`、`WEIGHTS_0` 和 24 组逆绑定矩阵。骨骼层级、骨骼 ID、绑定局部位置和绑定骨长均保持只读。
5. 下一次 Windows 实机检查必须从 V002 验证页进入。验证页通过后，编辑器左上状态卡应显示 `SKIN V002 唯一预绑定表皮已就绪`，详情应显示 `场景可见人体表皮 1 层`。
6. 单层问题通过实机截图确认后，继续审查肩、腋下、胯部、肘部、膝部、腕踝和躯干扭转。当前过渡实验权重仍需要专业权重和姿势修正形变替换。
7. 根目录 `launcher.ps1` 属于共享只读文件，本板块没有修改。总控合并时需要加入工作包路径、构建 ID、资源哈希和端口归属校验。
