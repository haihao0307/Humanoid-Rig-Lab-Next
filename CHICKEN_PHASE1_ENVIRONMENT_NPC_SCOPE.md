# Chicken Phase 1 — 基础世界环境生物 NPC

## 1. 阶段定位

本阶段不追求单只家鸡的全解剖复原，也不优先制作复杂生活史、繁殖、觅食生态或职业化行为。

阶段目标是得到一只可放入大型场景、能够稳定运行、外形可信、动作自然、可复制成群且彼此不穿透的基础环境生物 NPC。

优先级固定为：

1. 形态
2. 动作
3. 物理接触与稳定运行
4. 群体差异
5. 基础行为
6. 复杂生活活动与生态逻辑（后续阶段）

## 2. 当前基线

- 当前候选入口：`CHICKEN_V46_R9_8_3_CLEAN_FACE.html`
- 冻结回退：`CHICKEN_V46_R9_1.html`
- 当前头部仍处于视觉候选状态，尚未通过最终近景表面冻结。
- 身体、翼、尾、腿足模块继续冻结，除非整体轮廓审查证明存在会妨碍动作或地面接触的结构错误。

## 3. Phase 1 必须完成

### 3.1 形态基线

形态验收以场景 NPC 使用距离为主，不以博物馆级近景复原为标准。

必须满足：

- 3–5 米观察时，轮廓能立即读作真实家鸡，而不是几何鸟或长颈楔形动物；
- 头、颈、胸、腹、骨盆、尾和腿足的比例关系稳定；
- 左右侧、正面、顶部、三分之四和全身视图中没有明显断裂、悬浮、穿插或反向结构；
- 喙根、眼区、鸡冠和肉垂不妨碍后续头颈动作；
- 腿部关节位置能够支持走、停、转、短跑和地面适配；
- 运行时 LOD 允许删除近景微细节，但不得改变低频体型。

本阶段不要求：

- 精确到特定品种、年龄、性别和个体；
- 完整羽毛层级和逐根羽毛物理；
- 内部器官、肌肉全复原；
- 超近距离影视级皮肤与组织表现。

### 3.2 动作基线

动作优先采用真实骨链和固定骨长驱动，不允许用整体网格位移代替关节运动。

第一批动作必须完成：

1. `idle_stand`：稳定站立、轻微呼吸与重心微调；
2. `look`：头颈快速转向，身体只做必要补偿；
3. `peck`：颈部前伸、头部下探、喙触地后回收；
4. `walk`：左右脚交替、身体垂直起伏受控、头部具有鸡类步态中的补偿；
5. `stop`：从行走平滑减速，不突然锁死；
6. `turn`：小角度步进转向，不在原地整只旋转；
7. `short_run`：短距离加速和减速，用于避让与群体分离；
8. `wing_balance`：失衡或急转时的短暂展开与回收。

动作系统必须保留：

- 固定骨长；
- 父子层级；
- 左右脚接触事件；
- 根节点位移和局部关节旋转分离；
- 动作前后可回滚；
- 运行时低成本采样。

### 3.3 物理与碰撞

Phase 1 使用简化碰撞代理，不采用每片羽毛或完整网格碰撞。

必须包含：

- 身体主胶囊或椭球代理；
- 头部小型代理；
- 左右腿足地面接触；
- 个体之间的软分离和最小间距；
- 障碍物前的减速、绕行或转向；
- 碰撞后动作不中断，改为重新规划短路径；
- 不允许多只鸡长期重叠或相互卡死。

群体碰撞采用邻域查询、简化代理和分层更新；不得给每只鸡启用高成本全刚体模拟。

### 3.4 群体差异

单只 NPC 稳定后再扩展为群体。差异必须由受控参数生成，而不是无边界随机。

允许变化：

- 体型尺度；
- 胸腹丰满度；
- 腿长与步幅；
- 头部尺度；
- 鸡冠大小；
- 尾羽角度；
- 羽色参数；
- 行走速度；
- 动作相位；
- 警觉程度。

必须保持：

- 家鸡基本拓扑；
- 关节层级；
- 动作兼容性；
- 碰撞代理同步；
- 参数范围不会生成病态或穿插个体。

## 4. Phase 1 非目标

本阶段不投入主要精力到：

- 筑巢、产蛋、孵化和育雏；
- 完整觅食生态；
- 社会等级和繁殖行为；
- 声音语义系统；
- 复杂任务规划；
- 影视级近景毛羽物理；
- 特定历史品种的一比一复原。

这些能力在基础形态、动作、碰撞和群体稳定后进入后续阶段。

## 5. 执行顺序

### Gate A — 形态冻结

只修正会影响场景识别、动作和碰撞的低频结构。通过固定六视图和 3–5 米等效观察距离验收后冻结。

### Gate B — 单只动作闭环

完成 idle、look、peck、walk、stop、turn、short_run 和 wing_balance，并验证脚底接触、朝向更新、重心和循环边界。

### Gate C — 单只物理闭环

完成地面适配、障碍避让、软碰撞、受阻后继续任务和不穿透检查。

### Gate D — 小群测试

先测试 6–12 只，启用不同体型、速度和动作相位；检查拥堵、卡死、穿插、路径中断和性能。

### Gate E — 场景 NPC 基线冻结

形成可复用的 Chicken NPC 母体、参数配置、动作集、碰撞配置、LOD 配置和测试记录，之后再扩展其他相似动物。

## 6. 性能边界

第一阶段目标：

- 单只 NPC 在普通桌面浏览器中动作与碰撞更新稳定；
- 12 只小群不因彼此碰撞而明显掉帧或卡死；
- 远距离个体降低骨骼和行为更新频率；
- 群体成员共享几何、材质和动作数据；
- 个体差异通过参数和实例数据表达；
- 不按帧复制完整网格或完整状态；
- 近、中、远 LOD 的低频轮廓保持一致。

## 7. 当前结论

当前阶段不再把“全部头部细节完成”作为进入动作阶段的无限期前置条件。只要低频形态达到场景 NPC 可识别、可运动、可碰撞的门槛，就冻结形态并进入动作闭环；近景细节作为后续独立支线继续改进。
