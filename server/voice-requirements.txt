# Installed only in a separate per-user environment after explicit download consent.
# CPU inference. No CUDA, PyTorch, external model executable or OS-wide pip install.
faster-whisper==1.2.1
opencc-python-reimplemented==0.1.7
